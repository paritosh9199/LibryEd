<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.paritosh.libryed">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity"
            android:label="Login"/>
        <activity android:name=".RegistrationActivity" android:parentActivityName=".LoginActivity"
            android:label="Register"/>
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DashBoardActivity"
            android:theme="@style/Theme1"/>
        <activity android:name=".BooksActivity" android:parentActivityName=".DashBoardActivity"
        android:label="Books"/>
        <activity android:name=".SearchActivity" android:parentActivityName=".DashBoardActivity"
            android:label="Books Library"
            android:theme="@style/Theme1"/>
        <activity android:name=".HistoryActivity" android:parentActivityName=".BooksActivity"
            android:label="History">

        </activity>
    </application>

</manifest>